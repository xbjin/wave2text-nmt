
dropout_rate: 0.0
cell_size: 512
embedding_size: 512
layers: 2
bidir: True
use_lstm: True
steps_per_checkpoint: 1000
steps_per_eval: 4000
max_steps: 0
optimizer: 'adam'
learning_rate: 0.001
num_samples: 0

data_dir: experiments/news/data
model_dir: experiments/news/baseline
log_file: experiments/news/baseline/log.txt

encoders:
  - name: fr
    vocab_size: 30000

decoder:
    name: en
    vocab_size: 30000
